Flask==3.1.0
# RS256 access tokens + JWKS, and RFC 7523 private_key_jwt client assertions.
PyJWT==2.10.1
cryptography==44.0.0
# (client_secret and API-key hashing use the standard library: hashlib, secrets.)
