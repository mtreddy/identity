Flask==3.1.0
# RS256 access tokens + JWKS, RFC 7523 private_key_jwt client assertions, and
# ES256 DPoP proofs (RFC 9449) — all via PyJWT + cryptography.
PyJWT==2.10.1
cryptography==44.0.0
# (client_secret hashing uses the standard library: hashlib, secrets.)
