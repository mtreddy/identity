Flask==3.1.0
cryptography==44.0.0
# CBOR (de)serialization for COSE keys + the attestation object. The
# security-critical signature verification is done by hand on `cryptography`;
# cbor2 only handles the binary encoding.
cbor2==6.1.3
