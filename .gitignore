# Python / venv
.venv/
__pycache__/
*.pyc

# Local runtime artifacts (regenerated by seed.py / the server)
identity.db
*.log
.flask_session/
# OIDC signing key (generated locally; never commit private keys)
*.pem

# Test cookie jars
*.txt
!requirements.txt
