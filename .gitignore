# Python / venv
.venv/
__pycache__/
*.pyc

# Local runtime artifacts (regenerated by seed.py / the server)
identity.db
*.log
.flask_session/
# OIDC signing key (generated locally; never commit private keys)
*.pem
# mTLS demo PKI (generated locally; never commit CA/keys/certs)
certs/
*.crt
*.key

# Test cookie jars
*.txt
!requirements.txt
# SPIFFE SVIDs + trust bundle (generated locally)
svids/
app.db
# Dynamically-registered demo client id (26); stubbed email outbox (25)
demo_client.json
outbox.log
# Local dev secrets provisioned by bootstrap.py (never commit)
.dev-secrets.env
.dev-secrets/
