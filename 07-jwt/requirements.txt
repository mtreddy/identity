Flask==3.1.0
# JWT signing/verification.
PyJWT==2.10.1
# Needed only for the self-signed "adhoc" HTTPS option (USE_ADHOC_TLS=1).
cryptography==44.0.0
# (API-key hashing uses the standard library: hashlib, secrets.)
